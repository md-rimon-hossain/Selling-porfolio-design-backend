# Downloads Module Architecture

## 📊 Complete Endpoint Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      DOWNLOADS MODULE                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  🔒 ADMIN ENDPOINTS (Admin Role Required)                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ GET /downloads                                            │  │
│  │ • Get all downloads with advanced filters                │  │
│  │ • Search across users and designs                        │  │
│  │ • Pagination, sorting, statistics                        │  │
│  │ • Filters: type, user, design, date range                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ GET /downloads/analytics                                  │  │
│  │ • Get download analytics (daily/weekly/monthly/yearly)   │  │
│  │ • Top downloaded designs                                 │  │
│  │ • Overall statistics                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  👤 USER ENDPOINTS (Authentication Required)                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ GET /downloads/subscription-status                        │  │
│  │ • Check active subscription                              │  │
│  │ • View remaining downloads                               │  │
│  │ • Subscription details                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ GET /downloads/my-downloads                               │  │
│  │ • User's download history                                │  │
│  │ • Pagination and filtering                               │  │
│  │ • Sort by date or type                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ POST /downloads/design/:designId                          │  │
│  │ • Download a design file                                 │  │
│  │ • Verify purchase/subscription                           │  │
│  │ • Track download count                                   │  │
│  │ • Generate secure download link                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Download Flow Diagram

```
┌─────────────┐
│   CLIENT    │
│  (Frontend) │
└──────┬──────┘
       │
       │ POST /downloads/design/:designId
       │ + Auth Cookie
       ▼
┌──────────────────┐
│  AUTHENTICATION  │ ◄─── Verify JWT token from cookie
│    MIDDLEWARE    │
└────────┬─────────┘
         │
         │ User authenticated ✓
         ▼
┌──────────────────┐
│   VALIDATION     │ ◄─── Validate designId format (ObjectId)
│   MIDDLEWARE     │
└────────┬─────────┘
         │
         │ Valid ✓
         ▼
┌──────────────────────────────────────────────────────┐
│           DOWNLOAD CONTROLLER                         │
├──────────────────────────────────────────────────────┤
│                                                       │
│  1️⃣  Check if design exists and is active            │
│      ├─ Design.findById(designId)                    │
│      └─ Verify status === "Active"                   │
│                                                       │
│  2️⃣  Verify download permission                      │
│      ├─ Check individual purchase                    │
│      │   Purchase.findOne({                          │
│      │     user, design, type: "individual"          │
│      │   })                                           │
│      │                                                │
│      └─ OR check active subscription                 │
│          Purchase.findOne({                           │
│            user, type: "subscription",               │
│            status: "active",                         │
│            subscriptionEndDate: { $gt: now },        │
│            remainingDownloads: { $gt: 0 }            │
│          })                                           │
│                                                       │
│  3️⃣  Create download record                          │
│      new Download({                                   │
│        user, design, downloadType,                   │
│        purchase, downloadDate, ipAddress             │
│      }).save()                                        │
│                                                       │
│  4️⃣  Update counters (Atomic operations)             │
│      ├─ Design.findByIdAndUpdate(                    │
│      │     { $inc: { downloadCount: 1 } }            │
│      │   )                                            │
│      │                                                │
│      └─ If subscription:                             │
│          Purchase.findByIdAndUpdate(                 │
│            { $inc: { remainingDownloads: -1 } }      │
│          )                                            │
│                                                       │
│  5️⃣  Generate download link                          │
│      return {                                         │
│        downloadUrl: "/api/v1/files/...",             │
│        expiresAt: now + 15 minutes                   │
│      }                                                │
│                                                       │
└───────────────────┬───────────────────────────────────┘
                    │
                    │ Success Response
                    ▼
              ┌──────────┐
              │  CLIENT  │
              │ Download │
              │   File   │
              └──────────┘
```

---

## 🔍 Search Implementation Flow

```
┌──────────────────────────────────────────────────────────┐
│  GET /downloads?search=john&page=1                        │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│  Step 1: Find Matching Users                                │
│  ─────────────────────────────                              │
│  User.find({                                                │
│    $or: [                                                   │
│      { name: { $regex: "john", $options: "i" } },          │
│      { email: { $regex: "john", $options: "i" } }          │
│    ]                                                        │
│  }).distinct("_id")                                         │
│                                                             │
│  Returns: [userId1, userId2, userId3]                      │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│  Step 2: Find Matching Designs                              │
│  ──────────────────────────────                             │
│  Design.find({                                              │
│    $or: [                                                   │
│      { title: { $regex: "john", $options: "i" } },         │
│      { designerName: { $regex: "john", $options: "i" } }   │
│    ]                                                        │
│  }).distinct("_id")                                         │
│                                                             │
│  Returns: [designId1, designId2]                           │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│  Step 3: Query Downloads with Combined Filter               │
│  ─────────────────────────────────────────────              │
│  Download.find({                                            │
│    $or: [                                                   │
│      { user: { $in: [userId1, userId2, userId3] } },       │
│      { design: { $in: [designId1, designId2] } }           │
│    ]                                                        │
│  })                                                         │
│  .populate("user", "name email profileImage")              │
│  .populate("design", "title previewImageUrl price")        │
│  .sort({ downloadDate: -1 })                               │
│  .skip(0)                                                   │
│  .limit(20)                                                 │
│                                                             │
│  Returns: Matched downloads with populated data            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Filter Combinations

```
┌─────────────────────────────────────────────────────────────┐
│  EXAMPLE: Complex Filter Query                              │
└─────────────────────────────────────────────────────────────┘

GET /downloads?
  page=1
  &limit=25
  &search=dashboard
  &downloadType=subscription
  &startDate=2024-10-01T00:00:00.000Z
  &endDate=2024-10-31T23:59:59.000Z
  &sortBy=downloadDate
  &sortOrder=desc

┌─────────────────────────────────────────────────────────────┐
│  MongoDB Query Built:                                        │
│  ────────────────────                                        │
│  {                                                           │
│    downloadType: "subscription",        ◄─── Type filter    │
│    downloadDate: {                                          │
│      $gte: "2024-10-01T00:00:00.000Z", ◄─── Date range     │
│      $lte: "2024-10-31T23:59:59.000Z"                       │
│    },                                                        │
│    $or: [                                ◄─── Search        │
│      { user: { $in: [matchedUserIds] } },                  │
│      { design: { $in: [matchedDesignIds] } }               │
│    ]                                                         │
│  }                                                           │
│                                                              │
│  Sort: { downloadDate: -1 }              ◄─── Sorting       │
│  Skip: 0                                 ◄─── Pagination    │
│  Limit: 25                                                   │
└──────────────────────────────────────────────────────────────┘

RESULT: Downloads matching ALL criteria (AND logic)
        within the date range, subscription type,
        and matching search term
```

---

## 📈 Statistics Aggregation Pipeline

```
┌──────────────────────────────────────────────────────────────┐
│  Aggregation Pipeline for Statistics                         │
└──────────────────────────────────────────────────────────────┘

Download.aggregate([

  // Stage 1: Match filtered documents
  {
    $match: {
      downloadType: "subscription",
      downloadDate: { $gte: startDate, $lte: endDate }
    }
  },

  // Stage 2: Group and calculate
  {
    $group: {
      _id: null,
      totalDownloads: { $sum: 1 },

      individualDownloads: {
        $sum: {
          $cond: [
            { $eq: ["$downloadType", "individual_purchase"] },
            1,
            0
          ]
        }
      },

      subscriptionDownloads: {
        $sum: {
          $cond: [
            { $eq: ["$downloadType", "subscription"] },
            1,
            0
          ]
        }
      },

      uniqueUsers: { $addToSet: "$user" },
      uniqueDesigns: { $addToSet: "$design" }
    }
  },

  // Stage 3: Project final shape
  {
    $project: {
      _id: 0,
      totalDownloads: 1,
      individualDownloads: 1,
      subscriptionDownloads: 1,
      uniqueUsers: { $size: "$uniqueUsers" },
      uniqueDesigns: { $size: "$uniqueDesigns" }
    }
  }
])

┌──────────────────────────────────────────────────────────────┐
│  Result:                                                      │
│  {                                                            │
│    totalDownloads: 150,                                      │
│    individualDownloads: 65,                                  │
│    subscriptionDownloads: 85,                                │
│    uniqueUsers: 42,                                          │
│    uniqueDesigns: 38                                         │
│  }                                                            │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔐 Permission Check Logic

```
┌──────────────────────────────────────────────────────────────┐
│  checkDownloadPermission(userId, designId)                    │
└──────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Check 1: Individual Purchase                                │
│  ────────────────────────────                                │
│  Purchase.findOne({                                          │
│    user: userId,                                             │
│    design: designId,                                         │
│    purchaseType: "individual",                               │
│    status: "completed"                                       │
│  })                                                          │
│                                                              │
│  ✓ Found → ALLOW (unlimited downloads)                      │
│  ✗ Not found → Continue to Check 2                          │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  Check 2: Active Subscription with Downloads Available       │
│  ──────────────────────────────────────────────────────      │
│  Purchase.findOne({                                          │
│    user: userId,                                             │
│    purchaseType: "subscription",                             │
│    status: "active",                                         │
│    subscriptionEndDate: { $gt: new Date() },                │
│    remainingDownloads: { $gt: 0 }                           │
│  })                                                          │
│                                                              │
│  ✓ Found → ALLOW (decrement remaining)                      │
│  ✗ Not found → Continue to Check 3                          │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  Check 3: Subscription with No Downloads Left                │
│  ─────────────────────────────────────────────               │
│  Purchase.findOne({                                          │
│    user: userId,                                             │
│    purchaseType: "subscription",                             │
│    status: "active",                                         │
│    subscriptionEndDate: { $gt: new Date() },                │
│    remainingDownloads: { $lte: 0 }                          │
│  })                                                          │
│                                                              │
│  ✓ Found → DENY: "Download limit reached"                   │
│  ✗ Not found → Continue to Check 4                          │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  Check 4: Expired Subscription                               │
│  ──────────────────────────                                  │
│  Purchase.findOne({                                          │
│    user: userId,                                             │
│    purchaseType: "subscription",                             │
│    status: "active",                                         │
│    subscriptionEndDate: { $lte: new Date() }                │
│  })                                                          │
│                                                              │
│  ✓ Found → DENY: "Subscription expired"                     │
│  ✗ Not found → Continue to Final                            │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  Final: No Permission                                        │
│  ─────────────────────                                       │
│  DENY: "Need to purchase or subscribe"                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Schema Relationships

```
┌─────────────────┐         ┌─────────────────┐
│      USER       │         │     DESIGN      │
├─────────────────┤         ├─────────────────┤
│ _id             │◄───┐    │ _id             │◄──────┐
│ name            │    │    │ title           │       │
│ email           │    │    │ price           │       │
│ role            │    │    │ downloadCount   │       │
│ ...             │    │    │ status          │       │
└─────────────────┘    │    │ category        │       │
                       │    │ ...             │       │
                       │    └─────────────────┘       │
                       │                               │
                       │                               │
                       │    ┌─────────────────┐       │
                       │    │    PURCHASE     │       │
                       │    ├─────────────────┤       │
                       └────┤ user            │       │
                            │ design          ├───────┘
                            │ pricingPlan     │
                            │ purchaseType    │
                            │ status          │
                            │ remainingDownloads │
                            │ subscriptionEndDate │
                            └────────┬────────┘
                                     │
                                     │
                   ┌─────────────────┴─────────────────┐
                   │                                   │
         ┌─────────▼─────────┐            ┌───────────▼───────────┐
         │     DOWNLOAD      │            │    CATEGORY           │
         ├───────────────────┤            ├───────────────────────┤
         │ _id               │            │ _id                   │
         │ user          ───►├─┐          │ name                  │
         │ design        ───►├─┼──────────┤ isActive              │
         │ purchase      ───►├─┘          │ isDeleted             │
         │ downloadType      │            │ ...                   │
         │ downloadDate      │            └───────────────────────┘
         │ ipAddress         │
         │ userAgent         │
         └───────────────────┘

Key Relationships:
├─ User 1:N Download    (One user can have many downloads)
├─ Design 1:N Download  (One design can be downloaded many times)
├─ Purchase 1:N Download (One purchase allows multiple downloads)
└─ Category 1:N Design  (One category has many designs)
```

---

## ⚡ Performance Optimizations

```
1. Database Indexes
   ───────────────
   Download Collection:
   ├─ { user: 1, design: 1 }        ◄─── Compound index
   ├─ { user: 1, downloadDate: -1 } ◄─── User history queries
   └─ { design: 1, downloadDate: -1 }◄─── Design analytics

2. Query Optimizations
   ──────────────────
   ├─ .select() to exclude unnecessary fields
   ├─ .populate() only required fields
   ├─ .limit() for pagination
   └─ Aggregation pipelines for statistics

3. Atomic Operations
   ─────────────────
   ├─ $inc for counters (downloadCount, remainingDownloads)
   └─ Prevents race conditions

4. Caching Strategy (Future)
   ────────────────────────
   ├─ Cache user subscription status (5 min TTL)
   ├─ Cache download statistics (1 hour TTL)
   └─ Redis for download link generation
```

---

## 🚀 API Response Times (Target)

```
┌────────────────────────────────┬──────────────┬──────────────┐
│ Endpoint                        │ Target Time  │ Max Acceptable│
├────────────────────────────────┼──────────────┼──────────────┤
│ GET /downloads (with filters)  │ < 200ms      │ 500ms        │
│ GET /downloads/analytics       │ < 300ms      │ 800ms        │
│ GET /my-downloads              │ < 150ms      │ 400ms        │
│ GET /subscription-status       │ < 100ms      │ 300ms        │
│ POST /design/:id (download)    │ < 250ms      │ 600ms        │
└────────────────────────────────┴──────────────┴──────────────┘

Factors affecting performance:
├─ Database query complexity
├─ Number of populated fields
├─ Pagination limit size
└─ Search term complexity
```

---

For complete implementation details, see:

- `download.controller.ts` - Business logic
- `download.routes.ts` - Route definitions
- `download.validation.ts` - Input validation
- `download.model.ts` - Database schema
- `DOWNLOADS_API_DOCUMENTATION.md` - Complete API docs
